1. 扩展弹窗显示“最近下载”列表（限制 10–20 条），支持滚动与“更多”跳转管理页。
2. 下载分类新增“程序”分类，默认扩展名：`exe, msi, bat, cmd, ps1`。
3. 文件图标采用方案 A：按扩展名映射预置图标（如 PDF/WORD/IMG/ZIP/EXE）。
4. EXE 图标同样使用预置图标（不读取系统图标）。
5. 配置规则功能：可配置“文件类型 → 子文件夹名称”（仅下载根目录下子文件夹，不能自定义绝对路径）。
6. 规则导入导出：支持 JSON 格式导入/导出，包含校验与失败回滚。
